Creation of an admin user role
Assignment of the admin user role to ONLY jirving0311@gmail.com
Creation of a Users page that is viewable to only the admin. This page should list all users emails and riot ids.
The Users page should have a "permissions" feature per user that the admin can set. We should have some kind of default role where users can't modify global tags. Then a "global" (name subject to improvement) role where users can modify global tags. Admin role should exist but only for me for now.
Champion tags should have a "reviewed" boolean that displays on their Card. When editting tags, the user can check this to indicate that a human has reviewed and approved of the tags.
Creation of the Composition Requirements page that would handle the ability to create and modify composition requirements
